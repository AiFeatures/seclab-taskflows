name: Python CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    uses: Ai-road-4-You/enterprise-ci-cd/.github/workflows/ci-python.yml@v1
    secrets: inherit
